## Review for app.py


Hello! This is a well-structured Python script with good use of functions and modules. However, there are some suggestions that can improve the readability, maintainability, and best practices:

1. Use type hints for function arguments and return values to improve code readability and maintainability. For example, `def pdfFinderGUI()` could be updated to include type hints like this: `def pdfFinderGUI() -> None`. This will make the code more self-explanatory and easier to understand.
2. Use f-strings instead of formatting strings with `%` to improve readability and maintainability. For example, you can update the following line: `ttk.Label(mainframe, text=f"Die Datei oder Dateien wurden erfolgreich konvertiert und unter "{targetdir}" gespeichert").grid(column=2,row=2, sticky=(N))` to use f-strings like this: `ttk.Label(mainframe, text=f"Die Datei oder Dateien wurden erfolgreich konvertiert und unter {targetdir} gespeichert").grid(column=2,row=2, sticky=(N))`. This will make the code more readable and easier to maintain.
3. Use `os` module instead of `os.path.join` for file path concatenation. For example, you can update this line: `global pdfdirlist = []\nfor item in pdffilelist:\nif "pdf" in item:\npdfdirlist.append(os.path.join(pdfdirectory.replace("/", ("\\")), item))` to use `os` module like this: `global pdfdirlist = []\nfor item in pdffilelist:\nif "pdf" in item:\npdfdirlist.append(os.path.join(pdfdirectory, item))`. This will make the code more readable and easier to maintain.
4. Use `Tk` instead of `ttk` for creating GUIs. For example, you can update this line: `frame = Tk()` to use `Tk` like this: `frame = tk.Tk()`. Using `Tk` will make the code more readable and easier to maintain.
5. Use `try-except` block instead of checking for exceptions manually. For example, you can update this line: `global targetdir\ntargetdir = filename.replace(os.path.sep, ('\\'))` to use `try-except` block like this: `try:\n    global targetdir\ntargetdir = filename.replace(os.path.sep, ('\\'))\nexcept Exception as e:\n    print(f"Error occured while getting directory {filename}. {e}")`. Using `try-except` block will make the code more readable and easier to maintain.
6. Use `global` keyword sparingly to define global variables. Instead of using `global targetdir`, you can pass the value to a function as an argument or return it from a function. For example, you can update this line: `targetfolderGUI()` to use arguments like this: `def targetfolderGUI(filename):\ntargetdir = filename.replace(os.path.sep, ('\\'))`.
7. Use `enumerate` instead of indexing list items manually. For example, you can update this line: `for child in mainframe.winfo_children():` to use `enumerate` like this: `for i, child in enumerate(mainframe.winfo_children()):`. This will make the code more readable and easier to maintain.
8. Use `lambda` function instead of defining functions inline. For example, you can update this line: `def save(*args):\n    global targetdir\ntargetdir = patientnumberentry.get()` to use `lambda` function like this: `save = lambda:\n    global targetdir\ntargetdir = patientnumberentry.get()`. Using `lambda` function will make the code more concise and easier to maintain.
9. Use `os.path.sep` instead of hardcoding file path separator manually. For example, you can update this line: `pdfdirlist = []\nfor item in pdffilelist:\nif "pdf" in item:\npdfdirlist.append(os.path.join(pdfdirectory.replace("/", ("\\")), item))` to use `os.path.sep` like this: `pdfdirlist = []\nfor item in pdffilelist:\nif "pdf" in item:\npdfdirlist.append(os.path.join(pdfdirectory, item))`. This will make the code more readable and easier to maintain.
10. Use `print` statement instead of using `tkinter` for printing messages. For example, you can update this line: `ttk.Label(mainframe, text=f"Die Datei oder Dateien wurden erfolgreich konvertiert und unter "{targetdir}" gespeichert").grid(column=2,row=2, sticky=(N))` to use `print` statement like this: `print(f"The file or files were successfully converted and saved under {targetdir}.")`. Using `print` statement will make the code more readable and easier to maintain.

Overall, the code is well-structured and uses good practices, but there are some areas that can be improved for better readability, maintainability, and best practices.

---

